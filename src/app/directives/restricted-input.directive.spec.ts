import { RestrictedInputDirective } from './restricted-input.directive';

describe('RestrictedInputDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictedInputDirective();
    expect(directive).toBeTruthy();
  });
});
